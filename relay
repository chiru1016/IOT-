Relay Module + LED

#define RELAY 23   // Relay IN pin connected to GPIO23

void setup() {

  pinMode(RELAY, OUTPUT);

  // Relay OFF initially
  digitalWrite(RELAY, HIGH);

}

void loop() {

  // Turn ON relay (LED ON)
  digitalWrite(RELAY, LOW);

  delay(2000);

  // Turn OFF relay (LED OFF)
  digitalWrite(RELAY, HIGH);

  delay(2000);
}